# Normalize line endings on commit; check out LF everywhere.
# Set before the first external contributor, when it is still cheap.
* text=auto eol=lf

# Binary — never normalize
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
